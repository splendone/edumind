# 快速开始指南

本指南帮助你在5分钟内启动AI学习平台。

## 🚀 最快方式：Docker Compose（推荐新手）

### 前提条件
- 已安装 Docker
- 已安装 Docker Compose

### 启动步骤

```bash
# 1. 克隆项目（如果还没有）
git clone <repository-url>
cd webapp

# 2. 一键启动所有服务
docker-compose up -d

# 3. 查看启动日志
docker-compose logs -f

# 4. 等待服务启动完成（约1-2分钟）
```

### 访问系统

- **前端页面**: http://localhost:3000
- **后端API**: http://localhost:8080/api
- **数据库**: localhost:3306
- **Redis**: localhost:6379

### 默认账户

- **学生账户**: student1 / admin123
- **教师账户**: teacher1 / admin123
- **管理员**: admin / admin123

## 💻 开发者方式：本地开发

### 前提条件

确保已安装：
- JDK 17+
- Node.js 16+
- MySQL 8.0+
- Redis 7.x
- Maven 3.6+

### 步骤1：准备数据库

```bash
# 1. 启动MySQL
# 2. 创建数据库
mysql -u root -p
CREATE DATABASE ai_learning_platform DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;

# 3. 导入表结构
mysql -u root -p ai_learning_platform < sql/schema.sql
```

### 步骤2：启动Redis

```bash
# Linux/Mac
redis-server

# Windows
redis-server.exe
```

### 步骤3：配置后端

```bash
cd backend

# 编辑配置文件
vim src/main/resources/application.yml

# 修改以下配置：
# - 数据库密码
# - Redis配置（如果需要）
# - AI API密钥（可选，用于AI答疑功能）
```

**最小配置**：
```yaml
spring:
  datasource:
    password: your_mysql_password  # 修改这里
  data:
    redis:
      host: localhost
      port: 6379
```

### 步骤4：启动后端

```bash
# 方式1：使用Maven
mvn spring-boot:run

# 方式2：打包后运行
mvn clean package
java -jar target/ai-learning-platform-1.0.0.jar
```

后端启动成功标志：
```
AI Learning Platform Started Successfully!
API Documentation: http://localhost:8080/api/doc
```

### 步骤5：启动前端

打开新终端：

```bash
cd frontend

# 安装依赖（首次需要）
npm install

# 启动开发服务器
npm run dev
```

前端启动成功后，访问：http://localhost:3000

## 🧪 测试AI功能（可选）

### 配置AI API

1. 获取OpenAI API Key（或其他兼容的AI服务）
2. 编辑后端配置：

```yaml
ai:
  api:
    base-url: https://api.openai.com/v1
    api-key: sk-your-api-key-here
    model: gpt-4
```

3. 重启后端服务
4. 访问AI答疑页面测试

**注意**：如果不配置AI API，其他功能仍可正常使用，只有AI答疑功能会受影响。

## 📖 功能体验

### 1. 浏览课程
- 访问首页查看推荐课程
- 点击"课程"菜单浏览所有课程

### 2. 学习中心
- 登录后访问"学习中心"
- 查看学习进度、统计数据
- 生成学习报告

### 3. AI答疑
- 点击"AI答疑"菜单
- 选择相关课程（可选）
- 输入问题，体验智能问答

### 4. 在线测试
- 访问"在线测试"
- 选择试卷进行练习
- 查看错题本

## 🔧 常见问题

### Q1: 后端启动失败

**可能原因**：
- MySQL未启动或密码错误
- Redis未启动
- 端口8080被占用

**解决方法**：
```bash
# 检查MySQL
mysql -u root -p

# 检查Redis
redis-cli ping

# 检查端口占用
# Linux/Mac
lsof -i :8080
# Windows
netstat -ano | findstr :8080
```

### Q2: 前端无法访问后端

**检查**：
1. 后端是否正常启动
2. 访问 http://localhost:8080/api 是否有响应
3. 浏览器控制台是否有CORS错误

### Q3: 数据库连接失败

```bash
# 检查MySQL状态
systemctl status mysql   # Linux
brew services list        # Mac
services.msc             # Windows

# 检查配置
# backend/src/main/resources/application.yml
```

### Q4: Docker启动失败

```bash
# 查看详细日志
docker-compose logs backend
docker-compose logs mysql

# 常见问题：
# 1. 端口被占用 - 修改docker-compose.yml中的端口
# 2. 内存不足 - 增加Docker内存限制
```

### Q5: AI功能不可用

- 检查AI API配置是否正确
- 确认API Key是否有效
- 查看后端日志错误信息
- 临时不用AI功能，其他功能可正常使用

## 📚 下一步

### 学习文档
- [完整README](../README.md) - 项目完整介绍
- [API文档](API.md) - 接口详细说明
- [部署指南](DEPLOYMENT.md) - 生产环境部署
- [项目总结](PROJECT_SUMMARY.md) - 技术架构详解

### 开发建议
1. 熟悉项目结构
2. 阅读核心代码
3. 尝试添加新功能
4. 参与项目贡献

### 自定义开发
- 修改前端主题色
- 添加新的课程分类
- 扩展AI提示词模板
- 增加新的数据统计维度

## 🎯 快速测试清单

完成以下步骤，确认系统正常：

- [ ] 访问首页，查看课程列表
- [ ] 登录系统（student1 / admin123）
- [ ] 访问学习中心，查看统计数据
- [ ] 体验AI答疑功能
- [ ] 查看一个课程详情
- [ ] 更新学习进度
- [ ] 查看学习报告
- [ ] 退出登录

## 💬 获取帮助

- 查看项目README
- 阅读API文档
- 查看日志文件
- 提交GitHub Issue

## 🎉 开始使用

现在你已经准备好了！开始探索AI学习平台的强大功能吧！

**提示**：建议先使用Docker方式快速体验，熟悉后再尝试本地开发模式。

---

祝学习愉快！🚀
